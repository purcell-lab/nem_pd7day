[mypy]
# homeassistant-stubs target the Python version Home Assistant itself
# requires; older targets fail on the stubs' own syntax before checking a
# single file, which is what CI was silently doing under continue-on-error.
python_version = 3.13
warn_return_any = false
ignore_missing_imports = true
check_untyped_defs = true
