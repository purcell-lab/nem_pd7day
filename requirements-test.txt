# Pinned test and type-check toolchain for CI (issue #107). Floating versions
# turned a matplotlib text-metrics change into a surprise chart-placement
# failure; bump these deliberately.
pytest==9.1.1
pytest-asyncio==1.4.0
astral==3.2
numpy==2.4.6
matplotlib==3.11.1
PyYAML==6.0.1
mypy==2.3.1
homeassistant-stubs==2026.2.3
